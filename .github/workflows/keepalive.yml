name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: curl -sS --max-time 30 -o /dev/null -w "%{http_code}" https://carwash-api-ahl3.onrender.com/api/health
